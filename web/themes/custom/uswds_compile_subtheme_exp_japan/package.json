{
  "name": "uswds_gulp_subtheme-change-me",
  "version": "3.x",
  "description": "Dependencies for compiling the assets/styles",
  "main": "index.js",
  "scripts": {
    "watch": "ddev npx gulp watch",
    "compile": "ddev npx gulp compile",
    "test": "echo \"Error: no test specified\" && exit 1",
    "init": "ddev npx gulp init"
  },
  "dependencies": {
    "@uswds/uswds": "^3.13.0",
    "gulp-cli": "^3.1.0"
  },
  "devDependencies": {
    "@uswds/compile": "^1.3.1",
    "gulp": "^5.0.1"
  }
}
